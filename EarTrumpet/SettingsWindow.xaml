<Window x:Class="EarTrumpet.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EarTrumpet"
        xmlns:uc="clr-namespace:EarTrumpet.UserControls"
        mc:Ignorable="d"
        Name="WindowRoot"
        WindowStyle="None"
        Icon="Tray.ico"
        ShowInTaskbar="True"
        AllowsTransparency="True"
        SizeToContent="WidthAndHeight"
        Foreground="{DynamicResource WindowForeground}"
        Background="{DynamicResource WindowBackground}"
        Height="300" 
        MinWidth="360"
        Width="360">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="31" ResizeBorderThickness="4" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="4,1,4,1" />
        </Style>

        <SolidColorBrush x:Key="WindowForeground"
                         Color="White" />
        <SolidColorBrush x:Key="WindowBackground"
                         Color="Black" />
        <SolidColorBrush x:Key="HeaderBackground"
                         Color="Black" />

        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="Content" Value="&#xE8BB;" />
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="31"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{x:Null}" />
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>
    <Grid>

        <DockPanel>

            <Grid DockPanel.Dock="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock TextAlignment="Left" Padding="20, 8, 20, 8" Text="{Binding ElementName=WindowRoot,Path=Title}" />
                <Button x:Name="CloseButton" Grid.Column="1" Style="{DynamicResource CloseButtonStyle}" Click="CloseButton_Click">
                </Button>
            </Grid>

            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Bottom"/>

            <StackPanel 
                Orientation="Vertical"
                Margin="8">

                <GroupBox Header="App Defaults">
                    <StackPanel Orientation="Vertical">
                        <Button Click="AddApp_Click">Add App</Button>
                        <ItemsControl ItemsSource="{Binding DefaultApps}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Vertical">
                                            <TextBlock Text="{Binding DisplayName}" />
                                            <uc:AudioSessionControl 
                        Stream="{Binding}"
                        IconHeight="24"
                        IconWidth="24"
                        VolumeTextFontSize="18"
                        IconSource="{Binding Icon}"
                        IconText="{Binding IconText}"
                        IconTextSize="16"
                        IconTextFontWeight="Bold"
                        IconTextFontFamily="{Binding RelativeSource={RelativeSource Self},Path=FontFamily}"
                        IconBackground="{Binding Background}"
                        />
                                        </StackPanel>

                                        <Button Grid.Column="1" Click="RemoveApp_Click">X</Button>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </GroupBox>

                <!-- TODO localization -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center"
                               Margin="4">Hotkey:</TextBlock>
                    <Button Click="HotkeySelect_Click" Content="{Binding HotkeyText}" />
                </StackPanel>

            </StackPanel>


        </DockPanel>
        

    </Grid>
</Window>
