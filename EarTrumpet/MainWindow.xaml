<Window x:Class="EarTrumpet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:resx="clr-namespace:EarTrumpet.Properties"
        xmlns:userControls="clr-namespace:EarTrumpet.UserControls"
        Title="EarTrumpet"
        MaxHeight="600"
        Width="360"
        Icon="Application.ico"
        Topmost="True"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        WindowStyle="None"
        Deactivated="Window_Deactivated"
        PreviewKeyDown="Window_PreviewKeyDown"
        Foreground="{DynamicResource WindowForeground}"
        Background="{DynamicResource WindowBackground}"
		ResizeMode="NoResize"
        TextOptions.TextFormattingMode="Display"
        x:Name="VolumeWindow">
    <Window.Resources>

        <SolidColorBrush x:Key="WindowForeground"
                         Color="White" />
        <SolidColorBrush x:Key="WindowBackground"
                         Color="Black" />
        <SolidColorBrush x:Key="HeaderBackground"
                         Color="Black" />
        <SolidColorBrush x:Key="CottonSwabSliderTrackFill"
                         Color="#FF339933" />
    </Window.Resources>

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Background="{DynamicResource HeaderBackground}">
            <Button DockPanel.Dock="Right" Style="{DynamicResource OptionsButtonStyle}" Content="&#xE712;" Click="Button_Click" >
                <!--<Button.Triggers>
                    <EventTrigger RoutedEvent="Button.Click">
                        <EventTrigger.Actions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <BooleanAnimationUsingKeyFrames                                  
                                            Storyboard.TargetProperty="ContextMenu.IsOpen">
                                        <DiscreteBooleanKeyFrame KeyTime="0:0:0" Value="True" />
                                    </BooleanAnimationUsingKeyFrames>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger.Actions>
                    </EventTrigger>
                </Button.Triggers>-->
                <Button.ContextMenu>
                    <ContextMenu x:Name="PopupContextMenu">
                        <Separator />
                        <MenuItem Header="{x:Static resx:Resources.FullWindowTitleText}" Command="{Binding OpenEarTrumpetVolumeMixerCommand}" />
                        <MenuItem Header="{x:Static resx:Resources.LegacyVolumeMixerText}" Command="{Binding OpenLegacyVolumeMixerCommand}" />
                        <Separator />
                        <MenuItem Header="{x:Static resx:Resources.PlaybackDevicesText}" Command="{Binding OpenPlaybackDevicesCommand}" />
                        <MenuItem Header="{x:Static resx:Resources.RecordingDevicesText}" Command="{Binding OpenRecordingDevicesCommand}" />
                        <MenuItem Header="{x:Static resx:Resources.SoundsControlPanelText}" Command="{Binding OpenSoundsControlPanelCommand}" />
                        <Separator />
                        <MenuItem Header="{x:Static resx:Resources.SettingsWindowText}" Command="{Binding OpenSettingsCommand}" />
                        <MenuItem Header="{x:Static resx:Resources.TroubleshootEarTrumpetText}" Command="{Binding OpenDiagnosticsCommand}" />
                        <MenuItem Header="{x:Static resx:Resources.ContextMenuSendFeedback}" Command="{Binding StartAppServiceAndFeedbackHubCommand}" />
                        <MenuItem Header="{Binding AboutHeader}" Command="{Binding OpenAboutCommand}" />
                        <MenuItem Header="{x:Static resx:Resources.ContextMenuExitTitle}" Click="ExitMenu_Click" />
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
            <TextBlock Text="{Binding DefaultDevice.Device.DisplayName}" 
                       DockPanel.Dock="Left"
                               Padding="12,6,8,6" 
                               FontSize="15" 
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis" />
        </DockPanel>
        <ScrollViewer 
                    Grid.Row="2"
                    Grid.ColumnSpan="2"
                    VerticalScrollBarVisibility="Auto"
                    CanContentScroll="True"
                    PanningMode="VerticalOnly"
                    Focusable="False"                      
                    Margin="0">
            <Grid>
                <Grid x:Name="NoAppsPanel"
                                Margin="0,8"
                                Grid.Row="1" 
                                Height="50"
                                Visibility="{Binding NoAppsPaneVisibility}">
                    <TextBlock Text="{Binding NoItemsContent}"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center"
                                    FontSize="{DynamicResource {x:Static SystemFonts.MessageFontSizeKey}}"
                                    TextWrapping="Wrap" />
                </Grid>


                <StackPanel Orientation="Vertical">


                    <userControls:DeviceAndAppsControl Device="{Binding DefaultDevice}"
                                                Visibility="{Binding DeviceVisibility}"/>
                    
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
