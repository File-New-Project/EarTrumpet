<Window x:Class="EarTrumpet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:EarTrumpet.UserControls"
        Title="EarTrumpet"
        MaxHeight="600"
        Width="360"
        Icon="Application.ico"
        Topmost="True"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        WindowStyle="None"
        Deactivated="Window_Deactivated"
        PreviewKeyDown="Window_PreviewKeyDown"
        Foreground="{DynamicResource WindowForeground}"
        Background="{DynamicResource WindowBackground}"
		ResizeMode="NoResize"
        TextOptions.TextFormattingMode="Display"
        x:Name="VolumeWindow">
    <Window.Resources>

        <SolidColorBrush x:Key="WindowForeground"
                         Color="White" />
        <SolidColorBrush x:Key="WindowBackground"
                         Color="Black" />
        <SolidColorBrush x:Key="HeaderBackground"
                         Color="Black" />
        <SolidColorBrush x:Key="CottonSwabSliderTrackFill"
                         Color="#FF339933" />

        
        <SolidColorBrush x:Key="LargeButtonBackground"
                         Color="Red" />

        <Style x:Key="LargeButton" TargetType="Button">
            <Setter Property="MinHeight" Value="50" />
            <Setter Property="Margin" Value="3,0,3,0" />
            <Setter Property="FontSize" Value="10" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="#33FFFFFF"
                         SnapsToDevicePixels="True"                                                                   
                         Background="{TemplateBinding Background}">

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="{TemplateBinding Tag}"
                                           Margin="4"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="18"/>

                                <ContentPresenter 
                                Margin="2" 
                                VerticalAlignment="Bottom" 
                                Grid.Row="1"
                                RecognizesAccessKey="True" />
                            </Grid>


                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter Property="BorderThickness" Value="1" />
                </Trigger>
            </Style.Triggers>
        </Style>
        
    </Window.Resources>

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Background="{DynamicResource HeaderBackground}">
            <Button BorderThickness="0"
                    HorizontalContentAlignment="Stretch"
                    Foreground="{DynamicResource WindowForeground}"
                    Click="ExpandCollapse_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" >
                            <Setter.Value>
                                <SolidColorBrush Color="Transparent" />
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                         SnapsToDevicePixels="True"                                                                   
                         Background="{TemplateBinding Background}">
                                        <ContentPresenter RecognizesAccessKey="True" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" >
                                    <Setter.Value>
                                        <SolidColorBrush Color="#22FFFFFF" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>

                <Button.Content>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding DefaultDevice.Device.DisplayName}" 
                               Padding="12,8,8,8" 
                               FontSize="15" 
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis" />

                        <TextBlock Grid.Column="1" 
                               FontFamily="Segoe MDL2 Assets"
                               VerticalAlignment="Center"
                               FontSize="15"
                               Margin="8"
                               Text="{Binding ExpandText}" />
                    </Grid>
                </Button.Content>

            </Button>
        </Grid>
        <ScrollViewer 
                    Grid.Row="1"
                    Grid.ColumnSpan="2"
                    VerticalScrollBarVisibility="Auto"
                    CanContentScroll="True"
                    PanningMode="VerticalOnly"
                    Focusable="False"                      
                    Margin="0">
            <Grid>
                <Grid x:Name="NoAppsPanel"
                                Margin="0,8"
                                Grid.Row="1" 
                                Height="50"
                                Visibility="{Binding NoAppsPaneVisibility}">
                    <TextBlock Text="{Binding NoItemsContent}"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center"
                                    FontSize="{DynamicResource {x:Static SystemFonts.MessageFontSizeKey}}"
                                    TextWrapping="Wrap" />
                </Grid>


                <StackPanel Orientation="Vertical">


                    <local:DeviceAndAppsControl Device="{Binding DefaultDevice}"
                                                Visibility="{Binding DeviceVisibility}"/>
                    <Grid  Visibility="{Binding ExpandedPaneVisibility}"
                           Margin="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0"
                                Tag="&#xe15d;"
                                Content="Playback"
                                Style="{StaticResource LargeButton}"
                                Background="{DynamicResource LargeButtonBackground}"
                                Foreground="{DynamicResource WindowForeground}"
                                Click="PlaybackDevices_Click" />
                        <Button Grid.Column="1"
                                Tag="&#xe13a;"
                                Content="Communication"
                                Style="{StaticResource LargeButton}"
                                Background="{DynamicResource LargeButtonBackground}"
                                Foreground="{DynamicResource WindowForeground}"
                                Click="CommunicationDevices_Click" />
                        <Button Grid.Column="2"
                                Tag="&#xe115;"
                                Content="Settings"
                                Style="{StaticResource LargeButton}"
                                Background="{DynamicResource LargeButtonBackground}"
                                Foreground="{DynamicResource WindowForeground}"
                                Click="Settings_Click" />
                        <Button Grid.Column="3"
                                Tag="&#xe8ad;"
                                Content="Pop out"
                                Style="{StaticResource LargeButton}"
                                Background="{DynamicResource LargeButtonBackground}"
                                Foreground="{DynamicResource WindowForeground}"
                                Click="PopOut_Click" />
                    </Grid>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
